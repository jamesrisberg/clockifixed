---
title: System Overview
description: High-level system diagram of Clockifixed's architecture and data flow.
icon: map
---

# System Overview

Clockifixed sits between your application and Clockify's REST API, providing type safety, validation, and normalization.

## System Context

```mermaid
C4Context
    title Clockifixed — System Context

    Person(dev, "Developer", "Builds integrations with Clockify")

    System(clockifixed, "Clockifixed SDK", "TypeScript wrapper with 277 types, 164 endpoints, runtime validation")

    System_Ext(clockify_api, "Clockify API", "api.clockify.me — 108 paths, 166 operations")
    System_Ext(clockify_reports, "Clockify Reports API", "reports.api.clockify.me")

    Rel(dev, clockifixed, "Uses", "TypeScript")
    Rel(clockifixed, clockify_api, "Calls", "HTTPS/JSON")
    Rel(clockifixed, clockify_reports, "Calls", "HTTPS/JSON")
```

## Component Architecture

```mermaid
graph TB
    subgraph "Clockifixed Package"
        direction TB

        subgraph "Public API"
            FACADE[/"Clockify Class<br/>(Facade)"/]
        end

        subgraph "Endpoint Layer"
            direction LR
            E1["WorkspaceEndpoints"]
            E2["UserEndpoints"]
            E3["ProjectEndpoints"]
            E4["TimeEntryEndpoints"]
            E5["ClientEndpoints"]
            E6["InvoiceEndpoints"]
            E7["ExpenseEndpoints"]
            E8["SchedulingEndpoints"]
            E9["ReportEndpoints"]
            E10["... +13 more"]
        end

        subgraph "Infrastructure"
            HTTP["HttpClient"]
            RL["RateLimiter"]
            PAG["Paginator"]
            ERR["Error Types"]
        end

        subgraph "Type System"
            direction LR
            M["Models<br/>(143 types)"]
            R["Requests<br/>(110 types)"]
            F["Filters<br/>(22 types)"]
            Z["Zod Schemas<br/>(277 validators)"]
        end

        subgraph "Verification"
            VER["Schema Verifier"]
            ANOM["Anomaly Tracker"]
        end
    end

    FACADE --> E1 & E2 & E3 & E4 & E5 & E6 & E7 & E8 & E9 & E10
    E1 & E2 & E3 & E4 & E5 & E6 & E7 & E8 & E9 & E10 --> HTTP
    HTTP --> RL & PAG
    E1 & E2 & E3 -.-> Z
    VER --> Z
    VER --> ANOM

    style FACADE fill:#f59e0b,stroke:#d97706,color:#000
    style HTTP fill:#3b82f6,stroke:#2563eb,color:#fff
    style Z fill:#10b981,stroke:#059669,color:#fff
    style VER fill:#8b5cf6,stroke:#7c3aed,color:#fff
    style ANOM fill:#ef4444,stroke:#dc2626,color:#fff
```

## Module Dependency Graph

```mermaid
graph LR
    subgraph "Entry Points"
        INDEX["src/index.ts"]
    end

    subgraph "Client"
        CLOCK["clockify.ts"]
        HTTP_M["http.ts"]
        EP["endpoints/*"]
    end

    subgraph "Types"
        T_INDEX["types/index.ts"]
        T_MOD["types/models/*"]
        T_REQ["types/requests/*"]
        T_FIL["types/filters/*"]
    end

    subgraph "Tools"
        GEN["scripts/generate-types.ts"]
        SPEC["openapi.json"]
    end

    INDEX --> CLOCK & T_INDEX
    CLOCK --> HTTP_M & EP
    EP --> HTTP_M & T_INDEX
    T_INDEX --> T_MOD & T_REQ & T_FIL
    GEN --> SPEC
    GEN -.->|generates| T_MOD & T_REQ & T_FIL

    style INDEX fill:#f59e0b,stroke:#d97706,color:#000
    style GEN fill:#8b5cf6,stroke:#7c3aed,color:#fff
    style SPEC fill:#6366f1,stroke:#4f46e5,color:#fff
```

## Technology Stack

| Layer | Technology | Purpose |
|---|---|---|
| Language | TypeScript 5.x | Type safety, IDE support |
| Runtime validation | Zod 4.x | Schema validation, parsing |
| Testing | Vitest 4.x | Unit tests, verification suite |
| HTTP | Native `fetch` | No dependencies, Node 20+ |
| Spec source | OpenAPI 3.0.1 | Clockify's official spec |
| Documentation | Tome | This site |
